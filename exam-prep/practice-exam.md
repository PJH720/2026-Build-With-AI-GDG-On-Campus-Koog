**문제 1: 이진 탐색 트리의 기본 개념 및 삭제**

(a) 이진 탐색 트리의 핵심 속성 두 가지를 설명하고, 이 속성 때문에 발생하는 중요한 특징 한 가지를 서술하시오.
(b) 다음 주어진 이진 탐색 트리에서 노드 25를 삭제하는 과정을 시나리오별로 설명하고, 각 시나리오에 따른 최종 트리의 형태를 간략히 그리거나 설명하시오. (중위 후속자(inorder successor)를 이용하는 경우를 포함하여 설명)

```
        50
       /  \
      30   70
     / \   / \
    20 40 60 80
       /
      25
```

**문제 2: AVL 트리의 균형 유지 및 회전**

(a) AVL 트리가 일반적인 이진 탐색 트리(BST)에 비해 가지는 주요 장점은 무엇이며, 이를 위해 어떤 핵심 개념을 도입하는지 설명하시오.
(b) 다음 순서대로 숫자들이 AVL 트리에 삽입될 때, 숫자 50이 삽입된 후 트리의 균형이 깨지고 어떤 회전(들)이 발생하는지 그 과정과 최종 트리의 균형 상태를 설명하시오. (각 삽입 단계마다 밸런스 팩터를 계산하여 설명)

`20, 10, 30, 40, 5, 50`

**문제 3: AVL 트리 구현 코드 분석 및 수정**

제공된 `getBalance` 함수와 `rightRotate` 함수는 AVL 트리의 균형을 유지하는 데 중요한 역할을 합니다. 아래 `getBalance` 함수와 `rightRotate` 함수가 각각 가지는 문제점을 설명하고, AVL 트리의 올바른 동작을 위해 어떻게 수정해야 하는지 구체적인 코드 예시와 함께 서술하시오.

```cpp
// getBalance 함수 (현재 코드)
int getBalance(Node* node) {
    if (node == nullptr) return 0;
    return getHeight(node->right) - getHeight(node->left); // 문제점 1
}

// rightRotate 함수 (현재 코드)
Node* rightRotate(Node* y) {
    Node* x = y->left;
    Node* T2 = x->right;

    x->right = y;
    y->left = T2;

    // 문제점 2: 특정 로직이 누락되어 있음

    return x;
}
```